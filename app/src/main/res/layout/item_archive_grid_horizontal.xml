<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools">

  <data>

    <import type="android.view.View" />

    <import type="android.text.TextUtils" />

    <variable
      name="viewModel"
      type="com.mstf.pmdb.ui.main.archive.adapter.ArchiveItemViewModel" />

  </data>

  <androidx.cardview.widget.CardView
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/space_medium"
    app:cardCornerRadius="@dimen/space_large"
    app:cardElevation="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
      android:id="@+id/parent"
      android:layout_width="wrap_content"
      android:layout_height="match_parent"
      android:background="?selectableItemBackground"
      android:clickable="true">

      <ImageView
        android:id="@+id/poster"
        omdbImageUrl="@{!TextUtils.isEmpty(viewModel.poster) ? viewModel.poster : `N/A`}"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:background="?colorPrimaryVariant"
        android:src="@drawable/ic_foreground"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

      <TextView
        android:id="@+id/rate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/space_medium"
        android:background="@{(viewModel.imdbRate != null) ? @drawable/bg_imdb_rating_radius : (viewModel.rottenTomatoesRate != null) ? @drawable/bg_rotten_tomatoes_rating_radius : (viewModel.metacriticRate != null) ? @drawable/bg_metacritic_rating_radius : null, default = @drawable/bg_imdb_rating_radius}"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:minWidth="@dimen/space_massive"
        android:minHeight="@dimen/space_massive"
        android:paddingHorizontal="@dimen/space_small"
        android:text="@{(viewModel.imdbRate != null) ? String.valueOf(viewModel.imdbRate) : (viewModel.rottenTomatoesRate != null) ? (viewModel.rottenTomatoesRate+`%`) : (viewModel.metacriticRate != null) ? String.valueOf(viewModel.metacriticRate) : ``}"
        android:textColor="@{(viewModel.imdbRate != null) ? @color/black : @color/white}"
        android:textSize="@dimen/font_x_large"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/poster"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintLeft_toRightOf="@id/poster"
        app:layout_constraintRight_toRightOf="parent"
        tools:text="7.7" />

      <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/space_medium"
        android:layout_marginVertical="@dimen/space_small"
        android:ellipsize="end"
        android:fontFamily="serif"
        android:gravity="left"
        android:maxLines="5"
        android:text="@{viewModel.title}"
        android:textColor="@color/black"
        android:textSize="@dimen/font_medium"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/rate"
        app:layout_constraintLeft_toRightOf="@id/poster"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0"
        tools:text="Movie Title" />

      <TextView
        android:id="@+id/year"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/space_medium"
        android:fontFamily="serif"
        android:gravity="left"
        android:singleLine="true"
        android:text="@{`(`+viewModel.yearStart+`)`}"
        android:textColor="@color/grey_600"
        android:textSize="@dimen/font_medium"
        app:layout_constraintBottom_toTopOf="@id/rate"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintLeft_toRightOf="@id/poster"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/title"
        app:layout_constraintVertical_bias="0.0"
        tools:text="Movie Title" />

    </androidx.constraintlayout.widget.ConstraintLayout>

  </androidx.cardview.widget.CardView>

</layout>