<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto">

  <data>

    <import type="android.view.View" />

    <import type="android.view.inputmethod.EditorInfo" />

    <variable
      name="viewModel"
      type="com.mstf.pmdb.ui.main.home.add_movie_dialog.AddMovieViewModel" />

  </data>

  <RelativeLayout
    android:id="@+id/layout_search"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:alpha="@{viewModel.searchLayoutAlpha}"
    android:orientation="vertical"
    android:paddingLeft="16dp"
    android:paddingTop="24dp"
    android:paddingRight="16dp"
    android:paddingBottom="24dp"
    android:visibility="@{viewModel.searchLayoutVisibility}">

    <TextView
      android:id="@+id/txt_search_title"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_alignParentLeft="true"
      android:layout_alignParentTop="true"
      android:layout_alignParentRight="true"
      android:layout_gravity="center_horizontal"
      android:layout_marginBottom="16dp"
      android:gravity="center_horizontal"
      android:text="find your movie or swipe up to open a blank form" />

    <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@+id/txt_search_title"
      android:layout_alignParentLeft="true"
      android:layout_alignParentRight="true"
      android:gravity="center"
      android:orientation="horizontal"
      android:weightSum="5">

      <EditText
        android:id="@+id/edt_search_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="2dp"
        android:layout_weight="2"
        android:background="@drawable/bg_bottom_sheet_dialogs"
        android:gravity="center"
        android:hint="Title"
        android:inputType="text"
        android:padding="4dp"
        android:singleLine="true" />

      <EditText
        android:id="@+id/edt_search_id"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="2dp"
        android:layout_weight="2"
        android:background="@drawable/bg_bottom_sheet_dialogs"
        android:gravity="center"
        android:hint="IMDb ID"
        android:inputType="text"
        android:padding="4dp"
        android:singleLine="true" />

      <ImageView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:layout_weight="0.75"
        android:background="?android:selectableItemBackgroundBorderless"
        android:clickable="true"
        android:padding="4dp"
        android:src="@drawable/ic_search"
        app:tint="@color/grey_600" />

    </LinearLayout>

  </RelativeLayout>

</layout>
